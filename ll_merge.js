'use strict';

class Node {}

class LinkedList{}

let mergeLists = (l1, l2) => {
  let currentNode = l1.head;
  let newNode = l2.next;

  while(li2.head != null) {
    li2.next = li2.head.next;
    li.head.next = li2.next
  }
  return l1;
}
